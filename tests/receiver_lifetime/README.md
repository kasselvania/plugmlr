# Receiver lifetime regression (offline candidate)

Candidate: c56ee2174f17fee6188a68f8af74116465d17bdd; pinned Monome18b489399d01a9178e4667b849ec4368d72533db. Production quarantine stays unchanged. These tests are NOT yet native-qualified. Never load alongside another MLR instance; buses are global.

Build from this worktree with `python3 tests/build_receiver_lifetime.py --fixture NEW_ABSOLUTE_DIRECTORY --mode ui|lifecycle|campaign --monome-source EXISTING_MONOME_GIT_CHECKOUT`. Destination must not exist. Builder reads pinned Git objects; it does not launch a process, modify production, or use working-tree application edits.

Checker: `python3 tests/check_receiver_lifetime.py --fixture DIRECTORY --not-before OWNED_PROCESS_LAUNCH_EPOCH`. Always pass launch epoch in native runs. JSON stdout PENDING/PASS/FAIL, exit1 on FAIL; actual process/crash tracking belongs to external UI owner. PID absence alone is not clean shutdown. Quit is the last app-bound action.

UI: open generated check.pd (full original mlr, all16 original players, DAC/Grid retained). Wait setup region0.5..1.5, source90,target120,pitch0. Press Render and use copy once. No automatic render/audition in UI fixture. Checker finishes only after native loaded event + deferred cleanup + committed player readback + loaded owner metadata/source + matching WAV/manifest. This is one-adoption case, not300 qualification.

Lifecycle: check.pd runs five finite fresh test sessions: normal, duplicate completion, Cancel before deferred cleanup, manual selection of Sample2 before deferred cleanup, parent destruction with completed callback/pending clock. Each actual render creates .75second stereo output. Only normal/duplicate adopt; Cancel/manual/destroy must suppress adoption. completed counts actual loaded buffers with expected lifetime outcomes, not necessarily adoption (case is recorded). Wrapper invokes a test hook before candidate copy_loaded, which schedules a SURVIVING driver clock0; it does not delete in a receiver callback. Candidate callback schedules its own clock afterward. Driver acts only from its clock after callback-return; destruction clears the generated session canvas including all original players/owners. No candidate method body is changed. Event order is independently checked. Duplicate notification is deliberately synthetic AFTER a real import; synthetic events alone cannot satisfy load/source/manifest checks.

Campaign: check.pd runs exactly300 normal render/load/adoption completions. Each iteration instantiates sixteen original sample owners/players in a generated subcanvas, seeds only slots1/2, requires destination16, validates native callback survival/cleanup, loaded bounds/file and committed selection, then clears the ENTIRE test subcanvas from a surviving control clock. Next iteration is an independently fresh instance, so no slot occupancy reset/unload shortcut and no full-bank false pass. Each output must be unique. All instances retain ordinary destination player completion listener. No DAC or Grid in lifecycle/campaign root, no playback commands. Closing root is not automatic; campaign leaves stopped empty test container with campaign-done. Native assertions clear test container and stop on failure. Per operation15s; total campaign900s (lifecycle120s). At300, final fresh-instance cleanup precedes campaign-done; checker refuses partial count. External watcher is read-only; a shorter watcher deadline must not be confused with native deadline.

Instrumentation differences: copied sample-stretch gains appended method wrappers logging entry/return and pending state; copied player gains passive committed-ID tap and test editor-open bus; editor panel gains test command bridge. Observer adds listeners and synchronous read-only metadata queries, durable TSV writes; these affect timing and cannot establish uninstrumented performance. Lifecycle hook explicitly imposes the tested schedule. Original DSP/worker/loader bodies preserved. Input/source hashes, callback ordering, actual load and adoption, unique rendered identities and region metadata checked independently. Instrumented teardown is not a proof of every possible GUI closure race.

Offline tests validate checker rejection and source syntax/connections. Native crash/audio/lifecycle acceptance remains OPEN. Do not run300 until basic and lifecycle fixtures pass under sole UI operator.
